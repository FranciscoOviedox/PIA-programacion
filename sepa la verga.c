sepa la verga
    /*while(!feof(usuariosbin)){
            if (strcmp(usuario_ingreso,usuarios.nombre_usuario)==0&&strcmp(pass_ingreso,usuarios.pass)==0){
                x=1;
                switch(usuarios.status){
                case 1:return 1;
                    break;
                case 2:return 2;
                    break;
                case 0: system("CLS");
                    printf("Usuario bloqueado, favor de contactar al administrador del sistema\n");
                    intentos=0;
                    break;
                }
                break;
            fread(sizeof(struct Usuarios),USR,usuariosbin);
        }
    */
    /*
        if (x==1){
            intentos=4;
        }else if (intentos==2){
            system("CLS");
            printf("Usuario bloqueado, favor de contactar al administrador del sistema\n\n");
            fread(&users, sizeof(tusers),1,arch);
            users.type=0;
            int pos=ftell(arch)-sizeof(tusers);
            fseek(arch,pos,SEEK_SET);
            fwrite(&users, sizeof(tusers), 1, arch);
            break;
            intentos=0;
        }else{
            system("CLS");
            printf("Nombre de usuario o contrase%ca invalido, intente de nuevo!!!\n\n",164);
            intentos++;
        */
     